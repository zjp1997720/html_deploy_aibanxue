### Coze 插件 API 调试交接文档

**文档版本:** 1.0
**日期:** 2025-09-25
**问题负责人:** 大鹏

-----

#### 1\. 目标与背景

**目标:** 在 Coze (扣子) 平台中开发一个插件，该插件通过调用我们自研的 `html_deploy_aibanxue` 项目的 API，实现“输入 HTML 代码，返回部署后的公开 URL”的功能。

**项目基本信息:**

  * **项目名称:** `html_deploy_aibanxue`
  * **技术栈:** Node.js, Express, Sequelize, SQLite
  * **生产环境 URL:** `https://htmlshare.aibanxue.top`
  * **代码状态:** 本地开发、GitHub 远程仓库、线上生产环境的代码已确认完全同步。

-----

#### 2\. 问题现状描述

**核心问题:** 在 Coze 插件的测试界面，调用本项目的 API 时，服务器持续返回 `HTTP 404 Not Found` 错误，导致插件测试失败。

**预期行为:**

  * 向 API Endpoint 发起 `POST` 请求。
  * 服务器应返回 `HTTP 201 Created` 状态码。
  * 响应体 (Response Body) 为一个 JSON 对象，其中包含一个 `url` 字段，值为新创建页面的完整可访问链接。

**实际行为:**

  * 请求发往 `https://htmlshare.aibanxue.top/pages`。
  * 服务器返回 `HTTP 404 Not Found` 状态码。
  * 响应体为项目的标准“页面未找到”HTML 页面，而非预期的 JSON 数据。

-----

#### 3\. API 接口定义与配置

根据项目代码和 Coze 插件的配置，正确的 API 调用参数如下：

  * **请求方法 (Method):** `POST`
  * **请求 URL (URL):** `https://htmlshare.aibanxue.top/pages`
  * **请求头 (Headers):**
      * `Content-Type`: `application/json`
      * `X-API-KEY`: `[用户提供的有效API Key]`
  * **请求体 (Body):**
    ```json
    {
      "content": "<!DOCTYPE html>..."
    }
    ```

-----

#### 4\. 已进行的调试步骤与分析

##### 步骤一：API 路径从 `/` 修正为 `/pages`

  * **操作:** 最初在 Coze 中配置的 API 路径为 `/`。
  * **结果:** 返回 404 错误。
  * **分析:** 经检查项目核心路由文件 `app.js`，发现 `pages` 相关的路由是通过 `app.use('/pages', pagesRouter);` 语句挂载的。这证明了所有 `routes/pages.js` 中定义的接口都必须以 `/pages` 为前缀。因此，正确的 API 路径是 `/pages`，而不是 `/`。

##### 步骤二：使用修正后的路径 `/pages` 重新测试

  * **操作:** 将 Coze 插件配置中的 API 路径更新为 `/pages`，并重新运行测试。
  * **结果:** **依然返回 404 错误。**
  * **关键证据 (来自 Coze 的调试日志):**
      * `url: https://htmlshare.aibanxue.top/pages` (证明 Coze 请求的 URL 是正确的)
      * `headers: map[... X-Api-Key:[hg_...] ...]` (证明 API Key 已正确发送)
      * `request body: {"content":"..."}` (证明请求体格式和内容正确)
      * `http code: 404` (证明服务器确实返回了 "Not Found")

**结论:** Coze 端的配置和发出的请求已经完全正确。问题 100% 出在**服务器端**。

-----

#### 5\. 待排查的潜在原因 (给 AI IDE 的指令)

请 AI IDE 重点检查以下几个方面，以定位 404 问题的根源。请按优先级顺序进行排查：

##### **假设 1 (最高优先级): Node.js 应用进程未重启**

  * **怀疑:** 线上服务器的 Node.js 应用在代码更新后没有被重启。这导致仍在内存中运行的是旧版代码（其中 API 路径还是 `/`），因此无法识别对 `/pages` 的新请求。
  * **验证指令:**
    1.  检查项目是否使用 `PM2` 进行进程管理（依据：项目根目录下存在 `ecosystem.config.js` 文件）。
    2.  如果使用 PM2，请检查应用的 `uptime`（运行时长）。如果时长很长（超过上次代码部署的时间），则极有可能未重启。
    3.  **核心操作建议:** 执行 `pm2 restart [app_name]` 或 `pm2 restart all` 来强制应用加载最新代码。

##### **假设 2 (中等优先级): 反向代理 (Nginx) 配置问题**

  * **怀疑:** 作为反向代理的 Nginx 服务器配置不正确，没有将 `/pages` 的请求转发给后端的 Node.js 应用，而是自己处理了该请求，因找不到对应文件而返回 404。
  * **验证指令:**
    1.  定位并分析线上环境正在使用的 Nginx 配置文件 (例如: `/etc/nginx/sites-enabled/your_domain.conf`)。
    2.  检查 `location / { ... }` 配置块，确保 `proxy_pass` 指令正确地指向了 Node.js 应用的监听地址和端口 (如 `http://localhost:3000`)。
    3.  确认没有其他 `location` 规则（例如 `location /p { ... }`）错误地匹配并拦截了对 `/pages` 的请求。
    4.  检查 Nginx 的 `access.log` 和 `error.log`，确认对 `/pages` 的请求记录。

##### **假设 3 (低优先级): 生产环境变量或依赖问题**

  * **怀疑:** 生产环境的 `.env` 文件缺少必要的配置，或者 `node_modules` 依赖未正确安装，导致 Express 路由初始化失败或行为异常。
  * **验证指令:**
    1.  对比 `.env.example` 和生产环境的 `.env` 文件，确认所有必要的变量都已设置。
    2.  检查应用启动日志 (通过 `pm2 logs [app_name]`)，看是否存在任何与路由、中间件或依赖加载相关的错误或警告。

**请 AI IDE 基于以上信息，对开发环境中的代码和配置进行深度扫描和分析，并给出最终的诊断结论和解决方案。**