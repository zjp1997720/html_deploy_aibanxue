<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <%- include('partials/header') %>
  <title><%= title %></title>
</head>
<body>
  <div class="main-container">
    <header class="app-header">
      <div class="title-container">
        <h1 class="cyber-title">HTML-GO</h1>
      </div>
      <p class="app-description">分享AI生成内容的最佳方式</p>
    </header>

    <div class="code-input-area">
      <!-- 这里是红框位置，放置密码输入模块 -->
      <div class="centered-password-container">
        <div class="card password-card centered-password-card">
          <h2 class="card-title"><i class="fas fa-lock" style="margin-right: 10px; color: var(--accent);"></i>此内容已加密保护</h2>
          <p class="password-description">请输入5位数字密码以访问此内容</p>
          
          <% if (error) { %>
            <div class="error-message"><%= error %></div>
          <% } %>
          
          <form action="/view/<%= id %>" method="get" class="password-form">
            <div class="password-input-container">
              <input type="text" name="password" maxlength="5" class="password-input" placeholder="请输入5位数字密码" autocomplete="off" autofocus>
            </div>
            <button type="submit" class="cyber-btn cyber-btn-primary submit-password">
              <i class="fas fa-unlock-alt mr-1"></i>解锁内容
            </button>
          </form>
        </div>
      </div>
    </div>

    <%- include('partials/footer') %>
  </div>

  <script src="/js/particles.js"></script>
  <script src="/js/theme.js"></script>
</body>
</html>
